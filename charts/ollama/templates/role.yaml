apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "ollama.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ollama.labels" . | nindent 4 }}
rules:
  # Ollama doesn't need any Kubernetes API access
  # Empty rules = no permissions
  []
