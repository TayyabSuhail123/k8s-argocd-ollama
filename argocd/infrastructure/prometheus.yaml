apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  
  source:
    # Using official Prometheus community Helm chart
    repoURL: https://prometheus-community.github.io/helm-charts
    chart: kube-prometheus-stack
    targetRevision: 55.0.0  # Using older, more stable version
    helm:
      releaseName: prometheus
      skipCrds: true  # Skip CRD management - install manually to avoid annotation size issues
      timeout: 10m  # Increase timeout for large chart
      values: |
        # Lightweight Prometheus for local testing
        prometheus:
          prometheusSpec:
            resources:
              requests:
                memory: 400Mi
                cpu: 200m
              limits:
                memory: 800Mi
                cpu: 500m
            # Run on general worker node
            nodeSelector:
              role: general
            # Storage
            storageSpec:
              volumeClaimTemplate:
                spec:
                  accessModes: ["ReadWriteOnce"]
                  resources:
                    requests:
                      storage: 5Gi
            # Allow ServiceMonitor discovery in all namespaces
            serviceMonitorNamespaceSelector: {}
            serviceMonitorSelector: {}
        
        # Disable components we don't need for now
        alertmanager:
          enabled: false
        grafana:
          enabled: false
        kubeStateMetrics:
          enabled: true
        nodeExporter:
          enabled: false
        prometheusOperator:
          resources:
            requests:
              memory: 100Mi
              cpu: 100m
            limits:
              memory: 200Mi
              cpu: 200m

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
